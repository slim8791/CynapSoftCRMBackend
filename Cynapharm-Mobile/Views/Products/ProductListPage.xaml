<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Cynapharm_Mobile.ViewModels.Products"
             xmlns:models="clr-namespace:Cynapharm_Mobile.Models.Products"
             xmlns:controls="clr-namespace:Cynapharm_Mobile.Controls"
             x:Class="Cynapharm_Mobile.Views.Products.ProductListPage"
             x:DataType="vm:ProductListViewModel"
             Title="{Binding Title}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,*">

        <!-- ── Primary header ────────────────────────────────────────────── -->
        <Grid Grid.Row="0"
              BackgroundColor="{StaticResource Primary}"
              Padding="16,48,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0"
                   Text="Catalogue"
                   FontSize="18" FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center" />

            <!-- Search bar -->
            <Border Grid.Row="1"
                    BackgroundColor="#26FFFFFF"
                    StrokeShape="RoundRectangle 10"
                    Stroke="Transparent"
                    Padding="12,0"
                    HeightRequest="44"
                    Margin="0,10,0,0">
                <Grid ColumnDefinitions="20,*,Auto" ColumnSpacing="8">
                    <Label Grid.Column="0" Text="🔍" FontSize="14"
                           TextColor="#99FFFFFF" VerticalOptions="Center" />
                    <Entry Grid.Column="1"
                           Placeholder="Rechercher un produit…"
                           Text="{Binding SearchQuery}"
                           PlaceholderColor="#99FFFFFF"
                           TextColor="White"
                           BackgroundColor="Transparent"
                           ReturnType="Search"
                           FontSize="14"
                           Margin="0"
                           VerticalOptions="Center" />
                    <Grid Grid.Column="2"
                          VerticalOptions="Center" HorizontalOptions="Center"
                          IsVisible="{Binding SearchQuery, Converter={StaticResource IsNotNullOrEmptyConverter}}">
                        <Label Text="✕" FontSize="14" TextColor="#99FFFFFF"
                               HorizontalOptions="Center" VerticalOptions="Center" />
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding ClearSearchCommand}" />
                        </Grid.GestureRecognizers>
                    </Grid>
                </Grid>
            </Border>

            <!-- Offline banner -->
            <Border Grid.Row="2"
                    BackgroundColor="{StaticResource SecondaryLight}"
                    StrokeShape="RoundRectangle 8"
                    Stroke="Transparent"
                    Padding="12,8"
                    Margin="0,8,0,0"
                    IsVisible="{Binding IsOffline}">
                <Label Text="⚠️ Mode hors ligne — catalogue du dernier chargement"
                       FontSize="12" FontAttributes="Bold"
                       TextColor="{StaticResource SecondaryText}"
                       HorizontalOptions="Center" />
            </Border>
        </Grid>

        <!-- Error banner -->
        <controls:ErrorBanner Grid.Row="1"
                              Message="{Binding ErrorMessage}"
                              Margin="16,8,16,0" />

        <!-- ── Category filter chips ─────────────────────────────────────── -->
        <ScrollView Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalScrollBarVisibility="Never"
                    IsVisible="{Binding Categories.Count, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                    BackgroundColor="{StaticResource CardBackground}"
                    Margin="0,0,0,0">
            <HorizontalStackLayout Padding="16,10" Spacing="8"
                                   BindableLayout.ItemsSource="{Binding Categories}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Border Padding="14,7"
                                StrokeThickness="1"
                                Stroke="{StaticResource BorderColor}"
                                BackgroundColor="{StaticResource SurfaceBackground}">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="20" />
                            </Border.StrokeShape>
                            <Border.Triggers>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectedCategory}"
                                             Value="{Binding .}">
                                    <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                    <Setter Property="Stroke"          Value="{StaticResource Primary}" />
                                </DataTrigger>
                            </Border.Triggers>
                            <Label Text="{Binding .}" FontSize="13"
                                   TextColor="{StaticResource TextSecondary}">
                                <Label.Triggers>
                                    <DataTrigger TargetType="Label"
                                                 Binding="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectedCategory}"
                                                 Value="{Binding .}">
                                        <Setter Property="TextColor"     Value="White" />
                                        <Setter Property="FontAttributes" Value="Bold" />
                                    </DataTrigger>
                                </Label.Triggers>
                            </Label>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SetSelectedCategoryCommand}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </HorizontalStackLayout>
        </ScrollView>

        <!-- Loading indicator + search hint -->
        <VerticalStackLayout Grid.Row="3" Spacing="4">
            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               Color="{StaticResource Primary}"
                               HorizontalOptions="Center"
                               Margin="0,6" />
            <Label Text="Entrez au moins 3 caractères pour lancer la recherche"
                   FontSize="11"
                   TextColor="{StaticResource TextSecondary}"
                   IsVisible="{Binding ShowSearchHint}"
                   HorizontalOptions="Center"
                   Margin="16,0" />
        </VerticalStackLayout>

        <!-- Result count -->
        <Grid Grid.Row="4"
              ColumnDefinitions="*,Auto"
              Padding="20,4,20,2"
              IsVisible="{Binding ProductCount, Converter={StaticResource IsNotNullOrEmptyConverter}}">
            <Label Grid.Column="0"
                   Text="{Binding ProductCount, StringFormat='{0} produit(s)'}"
                   FontSize="12"
                   TextColor="{StaticResource TextSecondary}" />
            <ActivityIndicator Grid.Column="1"
                               IsRunning="{Binding IsSearching}"
                               IsVisible="{Binding IsSearching}"
                               Color="{StaticResource Primary}"
                               HeightRequest="16" WidthRequest="16" />
        </Grid>

        <!-- ── Product list ───────────────────────────────────────────────── -->
        <RefreshView Grid.Row="5"
                     Command="{Binding RefreshCommand}"
                     IsRefreshing="{Binding IsRefreshing, Mode=TwoWay}"
                     RefreshColor="{StaticResource Primary}">
            <CollectionView ItemsSource="{Binding Products}"
                            ItemSizingStrategy="MeasureFirstItem">

                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center"
                                         Spacing="12" Padding="32">
                        <Label Text="📦" FontSize="48" HorizontalOptions="Center" />
                        <Label Text="Aucun produit trouvé"
                               Style="{StaticResource EmptyStateStyle}" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.Header>
                    <BoxView HeightRequest="8" Color="Transparent" />
                </CollectionView.Header>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Product">
                        <Border Margin="16,5" Style="{StaticResource CardStyle}">
                            <Grid ColumnDefinitions="68,*" ColumnSpacing="14">

                                <!-- Product image / placeholder -->
                                <Border Grid.Column="0"
                                        WidthRequest="52" HeightRequest="52"
                                        BackgroundColor="{StaticResource PrimaryLight}"
                                        Stroke="Transparent"
                                        VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="10" />
                                    </Border.StrokeShape>
                                    <Border.Triggers>
                                        <DataTrigger TargetType="Border"
                                                     Binding="{Binding ImageUrl, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                                                     Value="True">
                                            <Setter Property="IsVisible" Value="False" />
                                        </DataTrigger>
                                    </Border.Triggers>
                                    <Label Text="Rx"
                                           FontSize="16" FontAttributes="Bold"
                                           TextColor="{StaticResource Primary}"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center" />
                                </Border>

                                <Border Grid.Column="0"
                                        WidthRequest="52" HeightRequest="52"
                                        Stroke="{StaticResource BorderColor}"
                                        StrokeThickness="1"
                                        VerticalOptions="Center"
                                        IsVisible="{Binding ImageUrl, Converter={StaticResource IsNotNullOrEmptyConverter}}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="10" />
                                    </Border.StrokeShape>
                                    <Image Source="{Binding ImageUrl}"
                                           Aspect="AspectFill"
                                           WidthRequest="52" HeightRequest="52" />
                                </Border>

                                <!-- Product info -->
                                <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                    <Label Text="{Binding Nom}"
                                           FontAttributes="Bold" FontSize="14"
                                           TextColor="{StaticResource TextPrimary}"
                                           LineBreakMode="TailTruncation" />
                                    <Label Text="{Binding Categorie}"
                                           FontSize="12"
                                           TextColor="{StaticResource TextSecondary}"
                                           IsVisible="{Binding Categorie, Converter={StaticResource IsNotNullOrEmptyConverter}}" />
                                    <Label Text="{Binding PrixUnitaire, StringFormat='{0:N3} TND'}"
                                           FontSize="14" FontAttributes="Bold"
                                           TextColor="{StaticResource Primary}"
                                           IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CanSeePrices}" />
                                    <Border Padding="10,4"
                                            BackgroundColor="{StaticResource PrimaryLight}"
                                            Stroke="Transparent"
                                            HorizontalOptions="Start"
                                            IsVisible="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CanSeePrices, Converter={StaticResource InvertedBoolConverter}}">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="8" />
                                        </Border.StrokeShape>
                                        <Label Text="Disponible" FontSize="11" FontAttributes="Bold"
                                               TextColor="{StaticResource Primary}" />
                                    </Border>
                                </VerticalStackLayout>
                            </Grid>

                            <Border.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.GoToDetailCommand}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.Footer>
                    <BoxView HeightRequest="16" Color="Transparent" />
                </CollectionView.Footer>

            </CollectionView>
        </RefreshView>

    </Grid>
</ContentPage>
